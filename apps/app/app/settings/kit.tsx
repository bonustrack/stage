/** Settings → Kit route — the @metro-labs/kit component gallery + theme color
 *  editor as its own Settings sub-page. Reached via /settings → "Kit" row. */

import { KitPage } from '../../components/system/KitPage';

export default function SettingsKitRoute(): React.ReactElement {
  return <KitPage />;
}
