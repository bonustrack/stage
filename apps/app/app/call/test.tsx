/** /call/test route — Phase 0 camera-preview proof for video calls. Reached
 *  from System → "Call test". */

import { CallTestScreen } from '../../components/call/CallTestScreen';

export default function CallTestRoute(): React.ReactElement {
  return <CallTestScreen />;
}
