/** System → About route — app version / commit / build metadata as its own page. */

import { AboutPage } from '../../components/system/AboutPage';

export default function SystemAboutRoute(): React.ReactElement {
  return <AboutPage />;
}
