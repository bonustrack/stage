/** System → Kit route — the @metro-labs/kit component gallery as its own page. */

import { KitPage } from '../../components/system/KitPage';

export default function SystemKitRoute(): React.ReactElement {
  return <KitPage />;
}
