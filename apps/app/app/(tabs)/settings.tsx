/** @file Settings route rendering the top-level System-style settings menu, reached from the LeftDrawer. */

import { SettingsMenu } from '../../components/settings/SettingsMenu';

/** Settings tab route rendering the top-level settings menu. */
export default function SettingsRoute(): React.ReactElement {
  return <SettingsMenu />;
}
