/** @file Empty Messenger index route stub kept so expo-router resolves the tab while its body renders in the shared pager. */

/** Empty Messenger route stub kept so expo-router resolves the index tab. */
export default function TabRoutePlaceholder(): null {
  return null;
}
