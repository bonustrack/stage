/** @file Empty `/wallet` route stub kept so expo-router resolves the tab while its body renders in the shared pager. */

/** Empty Wallet route stub kept so expo-router resolves `/wallet`. */
export default function TabRoutePlaceholder(): null {
  return null;
}
