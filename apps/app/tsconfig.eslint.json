{
  "_comment": "Lint-only TS project: extends the app tsconfig and additionally includes the bun:test suite so type-aware ESLint (projectService) has type information for test files too. Not used by `tsc --noEmit` (the typecheck task), which keeps its tighter src include.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["react", "react-native", "bun"]
  },
  "include": [
    "app/**/*.ts",
    "app/**/*.tsx",
    "components/**/*.ts",
    "components/**/*.tsx",
    "lib/**/*.ts",
    "lib/**/*.tsx",
    "modules/**/*.ts",
    "modules/**/*.tsx",
    "test/**/*.ts",
    "test/**/*.tsx"
  ]
}
