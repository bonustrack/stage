/** @file Re-exports the shared AccountRecord type from the framework-agnostic Stage SDK (@stage-labs/client) so existing app imports stay stable. */
export type { AccountRecord } from '@stage-labs/client/accounts/types';
