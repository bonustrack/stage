/** @file Re-exports the shared AccountRecord type from the Stage SDK so existing app imports stay stable. */
/** Shared account-record types moved into the framework-agnostic Stage SDK (@stage-labs/client). Re-exported so existing app imports stay stable. */
export type { AccountRecord } from '@stage-labs/client/accounts/types';
