/** Re-export shim: ENS resolution moved into the framework-agnostic Stage SDK
 *  (@metro-labs/client). Kept here so existing app imports stay stable. */
export { resolveEnsName } from '@metro-labs/client/api/ens';
