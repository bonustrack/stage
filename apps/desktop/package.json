{
  "name": "stage-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Stage desktop app for macOS, Linux and Windows, bundling the web UI",
  "homepage": "https://stage.box",
  "author": "Stage Labs",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "esbuild src/main.ts src/preload.ts --bundle --platform=node --target=node22 --format=cjs --external:electron --outdir=dist --out-extension:.js=.cjs",
    "build:ui": "node scripts/export-ui.mjs",
    "start": "bun run build && bun run build:ui && electron .",
    "dist": "bun run build && bun run build:ui && electron-builder --publish never",
    "typecheck": "tsc --noEmit",
    "test": "bun test test/"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "44.3.0",
    "electron-builder": "26.15.3",
    "esbuild": "0.28.1"
  }
}
