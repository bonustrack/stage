/** @file Re-export of the shared HistoryEntry message-envelope type from @stage-labs/client (canonical, shared with the mobile app) so apps/ui call sites keep their `../lib/types` import path. */

export type { HistoryEntry } from '@stage-labs/client/types';
