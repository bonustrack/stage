{
  "name": "@metro-labs/metro",
  "version": "0.1.0-beta.15",
  "private": false,
  "description": "Event-interception wire. Supervises train subprocesses in ~/.metro/trains/, multiplexes their JSON event stream onto stdout, and routes outbound action calls back via stdin. Per-platform code is written by the agent (or user) as train scripts — metro core is pure transport.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bonustrack/stage.git"
  },
  "homepage": "https://github.com/bonustrack/stage#readme",
  "bugs": "https://github.com/bonustrack/stage/issues",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "metro": "./dist/cli/index.js"
  },
  "exports": {
    ".": "./dist/cli/index.js",
    "./define-train": "./dist/define-train.js",
    "./schema": "./dist/schema.js"
  },
  "files": [
    "dist",
    "docs",
    "examples",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc",
    "lint": "eslint src/ examples/",
    "lint:fix": "eslint src/ examples/ --fix",
    "typecheck": "tsc --noEmit",
    "test": "tsc && METRO_STATE_DIR=\"$(mktemp -d /tmp/metro-test.XXXXXX)\" bun test test/"
  },
  "dependencies": {
    "pino": "^9.5.0",
    "pino-pretty": "^13.1.3",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "eslint": "^10.3.0",
    "typescript": "^5",
    "typescript-eslint": "^8.59.2"
  },
  "packageManager": "bun@1.3.9"
}
