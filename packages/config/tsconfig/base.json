{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "@stage-labs/config — shared TypeScript base for the pure-TS packages (packages/client, packages/kit). These are the exact compilerOptions both inlined; the per-package tsconfig keeps its own lib/jsx/paths/include deltas. Relocation only — no behaviour change.",
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ESNext", "DOM"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "verbatimModuleSyntax": false,
    "noImplicitReturns": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  }
}
