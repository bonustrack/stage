{
  "name": "@metro-labs/railgun-mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Self-contained, version-pinned Railgun mobile bridge: the embedded nodejs-mobile host (engine + Groth16 prover), the Expo config plugins, and the typed public bridge API. Consumers import ONLY the barrel - never nodejs-assets internals.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./plugin": "./plugin/index.js"
  },
  "scripts": {
    "gen-methods": "node ../../apps/app/scripts/gen-railgun-methods.mjs",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@stage-labs/client": "*"
  }
}
