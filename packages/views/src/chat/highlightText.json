{
  "type": "Row",
  "wrap": "wrap",
  "align": "baseline",
  "children": [
    {
      "$repeat": "segments",
      "$item": {
        "type": "Text",
        "value": "{{item.value}}",
        "size": "{{size}}",
        "color": "{{item.color}}",
        "background": "{{item.background}}",
        "fontSize": "{{fontSize}}",
        "lineHeight": "{{lineHeight}}"
      }
    }
  ]
}
