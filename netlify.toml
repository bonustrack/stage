[build]
  base = "."
  publish = "apps/ui/dist"
  command = "bun install --frozen-lockfile && bun --cwd apps/ui run build"

[build.environment]
  BUN_VERSION = "1.3.13"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
