{
  "name": "@metro-labs/mcp",
  "version": "0.12.0",
  "description": "Claude Code and Codex plugin: Telegram + Discord channel that surfaces inbound messages live in-session and lets the agent reply, react, and edit-message.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "start": "bun install --no-summary && bun src/server.ts",
    "tail": "bun install --no-summary && bun src/tail.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "discord.js": "^14.14.0",
    "pino": "^9.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "eslint": "^10.3.0",
    "typescript": "^5",
    "typescript-eslint": "^8.59.2"
  },
  "packageManager": "bun@1.3.9"
}
