diff --git a/lib/module/views/NativeStackView.native.js b/lib/module/views/NativeStackView.native.js
index c342e90bffde9ad5044b6167bdde2e19be219270..18e0771032bb51f82ea2ecd7e9c448fc51cdcbd5 100644
--- a/lib/module/views/NativeStackView.native.js
+++ b/lib/module/views/NativeStackView.native.js
@@ -367,9 +367,27 @@ export function NativeStackView({
     acc[route.key] = acc[route.key] || describe(route, true);
     return acc;
   }, {});
+  // METRO PATCH: native-stack v7 only forwards iOS-only gesture props
+  // (gestureEnabled/fullScreenGestureEnabled) and never passes rn-screens'
+  // own interactive `goBackGesture`/`screenEdgeGesture`/`transitionAnimation`
+  // to <ScreenStack>. Those are the ONLY props that drive the Android
+  // (Reanimated) interactive swipe-back that parallaxes the previous screen
+  // under the finger. We source them from the focused route's options and
+  // forward them, plus the screensRefs/currentScreenId that rn-screens'
+  // ScreenStack asserts on when goBackGesture is set. The interactive worklet
+  // is wired by <GestureDetectorProvider> (mounted in app/_layout.tsx).
+  const screensRefs = React.useRef({});
+  const focusedRoute = state.routes[state.index];
+  const focusedOptions = (descriptors[focusedRoute?.key]?.options) ?? {};
+  const goBackGesture = focusedOptions.goBackGesture;
   return /*#__PURE__*/_jsx(SafeAreaProviderCompat, {
     children: /*#__PURE__*/_jsx(ScreenStack, {
       style: styles.container,
+      goBackGesture: goBackGesture,
+      transitionAnimation: focusedOptions.transitionAnimation,
+      screenEdgeGesture: focusedOptions.screenEdgeGesture ?? false,
+      screensRefs: goBackGesture !== undefined ? screensRefs : undefined,
+      currentScreenId: goBackGesture !== undefined ? focusedRoute?.key : undefined,
       children: state.routes.concat(state.preloadedRoutes).map((route, index) => {
         const descriptor = descriptors[route.key] ?? preloadedDescriptors[route.key];
         const isFocused = state.index === index;
diff --git a/lib/typescript/src/types.d.ts b/lib/typescript/src/types.d.ts
index 2f1351a89f0f2e67e854dc81e392198326b71834..f8dc649974fc60a67e9a216bfca9dfc70480d1bc 100644
--- a/lib/typescript/src/types.d.ts
+++ b/lib/typescript/src/types.d.ts
@@ -504,6 +504,15 @@ export type NativeStackNavigationOptions = {
      * @platform ios
      */
     fullScreenGestureEnabled?: boolean;
+    /**
+     * METRO PATCH: rn-screens' OWN interactive back-gesture props, forwarded to
+     * <ScreenStack> by our NativeStackView patch. These (unlike the iOS-only
+     * gestureEnabled/fullScreenGestureEnabled above) drive the Android Reanimated
+     * interactive swipe-back that parallaxes the previous screen.
+     */
+    goBackGesture?: import('react-native-screens').GoBackGesture;
+    screenEdgeGesture?: boolean;
+    transitionAnimation?: import('react-native-screens').AnimatedScreenTransition;
     /**
      * iOS 18 and below. Controls whether the full screen dismiss gesture has shadow under view during transition.
      * The gesture uses custom transition and thus doesn't have a shadow by default. When enabled, a custom shadow view
